/*import * as axios from 'axios'
import Vue from 'vue'

//axios 默认配置
axios.defaults.baseURL = 'http://new.test.upinkji.com/api/';

//请求前拦截
axios.interceptors.request.use(function(config){
    return config;
},function(err){
    return Promise.reject(err);
});

//请求后截取
axios.interceptors.response.use(function(res){
    console.log(res)
    if(res.status == 200){
        return res.data;
    }else{

    }
},function(err){
    return Promise.reject(err);
})

export default axios
*/
